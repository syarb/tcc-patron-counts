on:
  workflow_dispatch: {}
  push:
    paths:
      - .github/workflows/flat.yml
  schedule:
    - cron: 0 * * * *
jobs:
  scheduled:
    runs-on: ubuntu-latest
    steps:
      - name: Setup deno
        uses: denoland/setup-deno@main
        with:
          deno-version: v1.x
      - name: Check out repo
        uses: actions/checkout@v2
      - name: Fetch data
        uses: githubocto/flat@v3
        with:
          http_url: https://docs.google.com/spreadsheets/d/e/2PACX-1vS3dDNdq7_r43CaeRlJO8ZPsnwHtL6VjkrF0m7UiQkoEaexpZiSu5iQ1rmNuBh0W3Lh_P7G3y4njXcw/pub?gid=0&single=true&output=csv
          downloaded_filename: 12. 2021 Fall Intersession.csv
